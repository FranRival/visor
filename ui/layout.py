class Layout:
    def __init__(self, root):
        self.root = root
        self.build()

    def build(self):
        # Crear todos los frames y widgets
        # Guardarlos como self.canvas, self.list_sub, etc
        pass